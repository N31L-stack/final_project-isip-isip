name: final_project
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^16.3.3
  timezone: ^0.9.0
  intl: ^0.19.0  # KEEP THIS ONE

  # UI Components
  cupertino_icons: ^1.0.8
  
  # Navigation (choose one)
  # go_router: ^13.1.0  # Latest stable
  # OR
  # auto_route: ^7.8.1  # With code generation
  
  # State Management (choose one)
  provider: ^6.1.1  # Simple and reliable
  
  # Forms & Validation
  flutter_form_builder: ^9.2.0
  
  # HTTP/API
  http: ^1.2.2  # OR dio: ^5.4.3
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Charts & Data Visualization
  fl_chart: ^0.66.0
  
  # Date/Time
  
  # Animations
  animations: ^2.0.8
  
  # Loading Indicators
  flutter_spinkit: ^5.2.0
  
  # Image Handling
  cached_network_image: ^3.3.0

  # Tables for admin
  data_table_2: ^2.4.1
  
  # Dialogs (CORRECTED VERSION)
  awesome_dialog: ^3.1.1  # This is the correct latest version
  
  # Icons
  font_awesome_flutter: ^10.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/profile1.jpg
    - assets/anime.jpg
    - assets/Ledesma_Conchito.png
    - assets/Me.jpg
    - assets/

   